version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "pnpm"
      include: "scope" # Dependabot usa (deps) o (deps-dev)
    assignees:
      - "devparada"

    open-pull-requests-limit: 4
    versioning-strategy: "increase"  # Actualiza a versiones minor o patch en el package.json
    
    groups:
      # Grupo 1: Solo dependencias de producción (dependencies)
      production-dependencies:
        dependency-type: "production"

      # Grupo 2: Solo dependencias de desarrollo (devDependencies)
      development-dependencies:
        dependency-type: "development"
